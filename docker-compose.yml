version: '3'
services:
  scarecrow:
    image: scarecrow:latest
    container_name: scarecrow
    restart: unless-stopped
    ports:
      - "<HOST_ADDRESS>:<HOST_PORT>:19159"
    volumes:
      - /path/to/save_folder:/opt
    command: server --auth